Solution to the 'trapped rainwater' problem written in Dogescript
using the same simple algorithm as my Kotlin code (which is highly inefficient)

```
npm install -g dogescript
dogescript rw.djs > rw.js
node rw.js
```
